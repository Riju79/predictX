name: CI/CD Pipeline - PredictX Soroban & Web App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  # ── JOB 1: Test & Build Soroban Smart Contracts ──
  contracts-ci:
    name: 🦀 Soroban Smart Contracts CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32v1-none, wasm32-unknown-unknown

      - name: Add Rust WASM Targets
        run: |
          rustup target add wasm32v1-none || true
          rustup target add wasm32-unknown-unknown || true

      - name: Cache Cargo Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build Soroban WASM Contracts
        run: |
          cargo build --target wasm32v1-none --release --workspace

      - name: Run Cargo Workspace Tests
        run: |
          cargo test --workspace

  # ── JOB 2: Build Next.js Frontend App ──
  web-ci:
    name: ⚡ Next.js Web App CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: 'predict-x/package-lock.json'

      - name: Install npm dependencies
        working-directory: predict-x
        run: npm ci

      - name: Build Next.js Web Application
        working-directory: predict-x
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1

  # ── JOB 3: CD Release & Mainnet Deployment Audit ──
  deploy-ready:
    name: 🚀 Production Mainnet Release Audit
    needs: [contracts-ci, web-ci]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Audit Deployed Contracts Summary
        run: |
          echo "=== PREDICTX MAINNET CI/CD PASSED ==="
          cat deployed-contracts-mainnet.json
